<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="appBarLayoutTheme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="toolbarPopupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="GikoshBorderlessButton" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/app_name</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:tint">@color/colorIcons</item>
    </style>

    <style name="GikoshButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/app_name</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:tint">@color/colorIcons</item>
    </style>

    <style name="GikoshButton.Flat">
        <item name="android:layout_marginLeft">@dimen/no_margin_btn</item>
        <item name="android:layout_marginRight">@dimen/no_margin_btn</item>
        <item name="android:layout_marginTop">@dimen/no_margin_btn</item>
        <item name="android:layout_marginBottom">@dimen/no_margin_btn</item>
    </style>


    <style name="METAuthField">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <!--<item name="met_primaryColor">@android:color/white</item>
        <item name="met_baseColor">@android:color/white</item>
        <item name="met_textColorHint">@android:color/white</item>
        <item name="met_floatingLabelTextColor">@android:color/white</item>-->
        <item name="met_textColor">@color/colorAccent</item>
        <item name="met_helperTextColor">@android:color/black</item>
        <item name="met_helperTextAlwaysShown">true</item>
        <item name="met_iconPadding">8dp</item>
        <item name="met_floatingLabel">highlight</item>
        <item name="android:imeOptions">actionNext</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <dimen name="no_margin_btn">-8dp</dimen>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
